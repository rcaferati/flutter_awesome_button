# API tool environment

This isolated manifest pins `dart_apitool` 0.23.2 without injecting its Analyzer dependency into the Flutter library graph. Run `dart pub get --directory tool/dart_apitool` after cloning. The generated lockfile is part of the release toolchain baseline.
